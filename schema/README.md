# Schema

One file per record kind. A record kind that is not described here is not yet
ready to be depended on by anything outside this repository.

Every dated record carries enough context to be read on its own, because the
directory it sits in will eventually be deleted and nothing should have to
reconstruct meaning from a path that no longer exists.
